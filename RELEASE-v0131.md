# One More? v0.13.1 · Standard release

正式 `main` 移除「试玩成长路线」测试模式，包括设置／图鉴入口、14张实验牌和6件实验抵押物的展示，以及实验实现和图像文件。正式牌池与图鉴回到80张牌、20件抵押物；普通版已确认的表现、操作与无限模式保持。

旧 `?lab=growth` 链接移除该参数并进入普通版，使用原普通存档；独立测试存档保留，不作清除或转写。成长实验及素材仍保存在 `web-polish`，本地测试入口继续可用。

默认构建只输出正式内容到 `dist/`。实验构建需显式运行 `npm run build:playtest`，输出到独立 `dist-playtest/`；发布不能直接复制源码目录。

验证：207项源码规则测试通过；正式构建牌池／模块边界检查通过，与源码普通版1540次受控规则转换一致（20种子，含十桌→无限及保存恢复；不是胜率测试）。独立Electron Chromium 152.0.7977.78检查桌面中文、手机英文，根入口及旧测试链接均无测试入口／实验图鉴，普通保存不变、继续翻牌与教程配对正常；源码六路线仍可进入。报告在 `.artifacts/standard-v0131/`。

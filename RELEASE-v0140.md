# One More? v0.14.0

## 跳桌与构筑

先掷骰、确认下一桌目标，装袋分数已达标时才能跳桌。仅第10桌必须游玩，第2～9桌允许连续跳桌；无限模式不跳桌。

跳桌放弃普通路线、构筑包和该桌临时骰子援助，改为提前展示、固定存档的一项随机奖励：免费删2张、免费附魔、随机永久装订3张或1件随机未拥有的普通抵押物。没有直接分数、计分倍率奖励。跳过的目标仍保留，之后继续掷骰提高目标。每跳一次，仅紧接着下一桌的骰子目标倍率临时+1，不累计，之后恢复原曲线，不乘已有累计目标。

新增复写台：消耗6分，永久复制1张非炸弹牌，继承附魔及成长，清除本桌状态。普通流程第2、5、8桌结束后可遇见；成长路线也加入事件池。

## 后期操作与新牌

- 叠叠饼：不可配对，同名数量越多，每张分数越高。
- 节拍器：在场时每翻出3张食材，本桌倍率翻倍。
- 清扫车：在场时每翻出3张食材，清理最早入桌的麻烦。
- 静音铃：配对不触发效果，已配对食材最终分数再翻倍。
- 连抽计数器：每连续翻出4张食材，本桌倍率翻倍；其他牌打断。
- 自动配对钳：第10桌通关固定奖励，默认关闭。开启后翻出食材自动配对；同名优先，其次万能，目标按入桌顺序。发现效果仍需玩家选择。现有通关及无限存档补发一次，典当后不重复补发。

计分栏显示基础分与倍率，增加不阻塞下一次操作的数字增长动画。新增牌与抵押物沿用纯色块海报图标。

## 两张工具调整

- 回菜盘：把1张桌面麻烦放到牌堆底；不改变原有牌序，不算清理或消耗。
- 回炉印：消耗2分装袋分数，拆开一个对子，两张均可重新配对；保留附魔与已有加分，不生成残渣。

## 验证

`npm run check` 通过：245项测试全部通过，网页构建成功。覆盖掷骰后跳桌、仅末桌必打、连续跳桌、随机奖励存档、仅下一桌生效的目标倍率及恢复、奖励与原子回滚、永久装订跨桌及存档、复制、精确计分、自动配对队列与炸弹死亡、旧存档补发、两张工具的新结算。

Codex 内置 Chromium 实机检查：中文桌面跳桌／永久装订／复制／通关奖励／工具目标选择；390×844英文手机界面连续抽牌、自动配对、倍率显示，没有页面横向溢出。测试使用8994端口的隔离存档，未修改原8888端口的玩家存档。

本次为本地验证版本，尚未推送或更新线上。新增数值路线仍需玩家长局试玩反馈；未将规则正确性测试当作整体平衡结论。


## 奖励演出与配对叠放

自动配对钳新增透明金色插画、短领取过场、旋转金色放射、闪星、扫光和奖励音效。演出可跳过，获得与展示状态分别保存；演出结束后通关页保留开关位置提示。减少动态效果时缩短过场并停用闪星。

主菜单解锁簿增加新内容标记；解锁簿顶部展示未获得／已获得、每局十桌奖励条件及操作提示，旧通关记录兼容。此展示不改变奖励取得时机。

两张配对卡牌作为一个展示组排版，滑动靠拢后错落叠放；两张实体仍可独立选中，选中下层牌会置于上层。对子不跨行或跨页，手机仍为连续横向滑动；拆对或消耗后自动恢复独立排版。手动及自动配对共用显示规则。

追加验证：中文桌面真实通关领取／解锁簿，英文390×844自动配对／下层点选，以及回炉印拆对恢复。238项测试和构建通过，新增4项展示分组及奖励持久化测试。证据在 `.artifacts/reward-pairs-v0140/`。


## 本次修订：摇签、跳桌与统一图标

- 摇签保证换走原顶牌／整叠，单独剩一张或一叠除外；其他炸弹仍能被摇到顶部，开局保护不会重置。中英文道具和教程文案已同步。
- 跳桌奖励从四类有效奖励中随机抽取一类并存档，玩家只能决定是否跳桌，不能改选服务；删牌目标和附魔种类仍可选择。第2～9桌允许连续跳，第10桌不可跳。
- 每次跳桌只给下一桌骰点目标倍率临时+1；倍率为原曲线+1，再下一桌恢复原倍率，连续跳桌也不叠加，已确认目标与已装袋分数不变，无限模式仍为累计目标每桌×2。
- 自动配对钳全入口恢复 poster-art.js 的原生 SVG；旧 PNG 留存但不用于显示。奖励金光与配对叠放保留。
- 修复连续跳桌期间永久装订牌的存档校验：未正式开桌时不把待重装订的牌叠误判为损坏。
- 新验证使用独立8996端口，未操作8994/8995或8888的真实玩家存档。Codex内置Chromium：中文桌面跳2再准备跳3、刷新奖励不变、骰点17×2使目标26→60、摇走已知炸弹后实际翻出饭团、SVG通关演出；390×844中英文手机奖励界面无横向溢出。截图与日志见 .artifacts/skip-shuffle-v0140/。

# v0.8.0 验证记录

`npm run check`：127项通过，构建通过。新增检查覆盖多炸弹增长阈值、临时牌不计数、临时牌转永久、炸弹不会因删牌减少、不能移除或伪装炸弹、存档不重复追加，以及均匀洗牌不再限制前32张。已有双骰锁定、奖惩、80种卡牌结算与教学检查仍通过。

研究用规划器包含两项额外检查：六张明牌局面找到18分并与穷举一致；更改未知牌序、身份和随机状态不改变当前动作。模拟规模与局限见 [BALANCE.md](BALANCE.md)。

## 实际浏览器操作

应用内浏览器，隔离的 localhost 测试源；未替换127.0.0.1上的正常游戏存档。

- 390×844手机视口：140个实际规则操作，加上选牌和翻页，完成完整十桌；种子38，207／207分通关，9次路线、6次双骰加码，最终2枚炸弹。
- 多炸弹构筑预告：38张普通永久牌时，三个3张牌构筑包都提前显示下桌追加1枚炸弹；确认后进入第五桌，动画显示“炸弹×2 · +1”，存档中2枚，首张仍安全。
- 手机拖动时牌桌行实际跟随90像素；松手进入下一页。短拖22像素回位，第一页向右拖动发生阻尼并回弹。无残留位移或误选，单纯翻页前后存档一致。
- 已选择配对后跨页选另一张牌成功。完整流程进一步验证第三页回到前面寻找目标与发现工具。
- 1440×900桌面：80张上桌时点击翻页，观察到4行正在运行的过渡动画；结束后页码正确，存档不变。
- 英文当前牌组面板：99张普通永久牌、5枚炸弹，正确显示每增加20张与距离下一枚还差20张。
- 关闭动画时完整手机流程仍正常；英文及中文牌组增长说明和构筑预告已检查。

截图与实际操作报告：`.artifacts/smoke-one-more-v080/`。大牌组风险独立校验还包括10,000次真实引擎洗牌，不只是独立概率脚本。

## 验证边界

当前浏览器工具没有可用Edge接口，独立Edge启动在此前同环境已被自动审批阻止；本版使用应用内浏览器，没有将历史Edge结果当作本版通过。保留并更新了Edge检查脚本，但本版未执行其完整套件。

手机为浏览器视口检查，拖动通过鼠标Pointer Events验证；未声称测试了真机iOS／Android触屏、系统手势或地址栏收缩。蒙特卡洛的策略胜率不是人类胜率，也不是最优解证明。


## v0.8.2 首次遗物奖励推进修复（2026-09-14）

复现：v0.8.1 在1115×898视口中，首次遗物奖励的下一桌按钮底边位于1075像素，整页高度1115像素；按钮在屏幕外。滚动并选完卡包和遗物后可以进入第三桌，没有复现引擎死锁。

修复：卡包、遗物分步展示，已领取奖励显示回执；选择列表独立滚动，下一桌按钮常驻底部，并显示当前待完成的选择。沿用存档格式，没有修改炸弹和生命规则。

验证：`npm run check` 134项通过。新增回归覆盖全部三件遗物、三个候选卡包、旧版两种领取顺序、各选择边界恢复、重复领取被拒绝及九次轮末推进。

应用内浏览器150项检查通过：1280×720、960×600、390×700、844×390，中英文、全部三件遗物、领取后重载，以及旧版先领遗物的存档。可见性检查在点击前执行，不依赖自动滚动，并检测底部按钮不遮挡列表且可命中。从第三桌连续操作到第十桌，经过子夜事件并进入胜利界面；为专门验证推进使用了1000分银行余额的隔离存档，不代表正常平衡测试或正常玩家通关。正常动画下再次翻中炸弹立即失败，动画结束后界面可操作。

截图和JSON：`.artifacts/smoke-one-more-v082/`。所有测试使用localhost隔离源，未改动127.0.0.1的正常存档。仍使用应用内浏览器；Edge接口不可用且此前启动被自动审批拒绝，没有声称执行了本版Edge套件。保留的Edge脚本已同步版本、隔离源及无需滚动的按钮可见性检查。

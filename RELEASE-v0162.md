# One More? v0.16.2 — 锁链卡包、压牌与封包

## 牌局与奖励

- 累计装袋分数保留，收摊仍按累计目标判定成败。旧存分不解锁本桌增牌卡包；未解锁时可以收摊过桌，照常进入事件，但没有增牌卡包。抵押物的原有发放节点保留。
- 新局左侧先显示 X 形锁链与卡包进度：首桌 8 分，后续为 `max(8, 本桌已接受的骰点合计)`。计算本桌桌面得分加本桌装袋净增分，解锁后不会因消耗或封包重新上锁。炸弹死亡仍然没有结算奖励。
- 达线播放断链及三张捆绑卡牌的奖励演出，随后进度栏显示累计目标。成功收摊、完成事件后拆开卡包，展示三个有明确信息的候选包，选一个加入牌库；刷新不重抽候选，也不能重复领取。第十桌的普通构筑流程在选择继续无限后进行。
- 普通难度每次目标增加「固定底数 + 一颗 d20」。第 2–10 桌的底数暂定为 `4, 4, 6, 8, 10, 14, 18, 24, 32`。这是供试玩调整的第一版参数，不是已经验证的最终平衡值。进阶额外骰子与加码、跳桌仅下一桌骰点倍率 +1、无限目标翻倍保留。

## 压牌、封包与事件

- 压牌台：两张同名永久食材合成一张；两张的计分权重相加后乘 2，保留先选的主牌附魔，能力仍触发一次。两张普通 2 分食材成为一张 8 分食材。压牌权重永久保存，永久复制保留权重；临时复制原版仍按原版规则。
- 新工具「封袋绳」：将桌上一张永久散食材和一件已横置永久工具封包，每桌一次。原牌离开桌面，食材本桌不计分；下次实际入座时自动拆包，两张原牌就绪上桌，保留永久强化。封包不是消耗，不进垃圾桶、不生成残渣；跳桌期间封包继续保留。
- 所有事件结算场景增加贿赂开关，明确升级效果与总分数费用。切换、选目标、返回均不扣分；确认后一次性验证和结算。包括双份附魔、双删、双复制、减量换牌、永久装订、提高压牌倍率、加倍典当报价、加强下桌补给、赎回没收牌与解除临时负面规则。
- 加入断链、卡包打开与展开、压具合牌、封袋和开场自动拆包动画及同步音效。使用原有纯色块色盘与原生 SVG；旧素材保留。动画可跳过，支持简化动态设置。

## 存档与测试

- 实际新游戏启用 `economy: 2`。已有进行中存档不回收当前桌奖励，不改当前目标、牌序或存分，从下一张牌桌启用；教学保留原有流程，完成后衔接新规则。没有清除玩家存档。
- `npm run check`：330 项测试通过，静态构建成功。其中 18 项新增规则测试覆盖卡包锁定、永久合牌、费用原子性、各类贿赂、封包跳桌与刷新、十桌到无限以及旧存档衔接。
- 使用独立端口 9022、隔离 Edge Chromium 配置验证桌面中文与手机英文流程，未修改 8888 的玩家存档。Codex 浏览器连接工具返回连接错误，因此浏览器自动检查通过本地 Edge 完成。
- 实机画面与正常速度演出记录：`.artifacts/economy-v0162/`。玩法入口仍为 `http://127.0.0.1:8888/`；独立演示入口为 `http://127.0.0.1:9022/.artifacts/economy-v0162/index.html`。
- 本次为本地源码和静态构建，尚未推送或更新线上、itch、Windows 安装包。
